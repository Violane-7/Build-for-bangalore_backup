// Dev 3: Goal planner — "What If" simulator + milestone timeline

export default function GoalPlannerForm() {
  return (
    <div>
      {/* TODO: Dev 3 — Build goal planner here
          - Goal input (text + target date)
          - POST to /api/health/goal-plan → AI returns milestones
          - Weekly milestone timeline chart
          - "What If" param tweaker (e.g. change diet, sleep)
          - Projected biological age improvement
      */}
      <p>Goal Planner Form Component</p>
    </div>
  );
}
