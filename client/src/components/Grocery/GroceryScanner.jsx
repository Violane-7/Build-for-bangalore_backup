// Dev 3: Grocery scanner — camera/upload + AI nutrition analysis

export default function GroceryScanner() {
  return (
    <div>
      {/* TODO: Dev 3 — Build grocery scanner here
          - Camera capture button (getUserMedia)
          - File upload fallback
          - Preview scanned image
          - Call /api/grocery/scan with image
          - Render per-item nutrition cards
          - Recommendations ("reduce sugar", "add fiber")
          - Scan history list
      */}
      <p>Grocery Scanner Component</p>
    </div>
  );
}
