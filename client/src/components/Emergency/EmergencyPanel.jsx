// Dev 1: Emergency action panel
// Audio alarm, vibrate, auto-call, message contacts, instructions

export default function EmergencyPanel() {
  return (
    <div>
      {/* TODO: Dev 1 — Build emergency action here
          - Big SOS button (hold to trigger)
          - Web Audio API alarm on trigger
          - navigator.vibrate() pulse
          - Countdown before auto-call 911 (tel: link)
          - POST to /api/users/emergency-notify to message contacts
          - Display emergency instructions based on type
            e.g. "Chew 325mg aspirin" for cardiac
          - Cancel button with timer
      */}
      <p>Emergency Panel Component</p>
    </div>
  );
}
